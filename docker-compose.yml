version: '3.8'
services:
  keydb:
    image: eqalpha/keydb
    container_name: mykeydb
    ports:
      - "6379:6379"
    volumes:
      - keydb-data:/data
    command: keydb-server /etc/keydb/keydb.conf
    restart: unless-stopped

volumes:
  keydb-data: